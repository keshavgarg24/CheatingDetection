# Core dependencies - ensure compatibility with Python 3.11
opencv-python>=4.8.0,<5.0.0
Flask>=2.3.0,<4.0.0
numpy>=1.24.0,<2.0.0

# MediaPipe (requires Python 3.9-3.11)
mediapipe>=0.10.0

# For phone detection
ultralytics>=8.0.0
torch>=2.0.0
torchvision>=0.15.0

# Face recognition and identity verification
face-recognition>=1.3.0
Pillow>=8.0.0

# Enhanced dashboard with WebSocket support
flask-socketio>=5.3.0
python-socketio>=5.10.0
flask-cors>=4.0.0

# Optional: For audio monitoring (may need system dependencies)
# pyaudio>=0.2.11